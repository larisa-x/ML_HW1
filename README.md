1) Работа с данными и моделями

**Часть 1 | EDA и визуализация**

Посмотрели на данные, почистили их - удалили пропуски по target, дубликаты заполнили медианами для количественных признаков, поработали с форматами
Также посмотрели на корреляции, выявили возможные зависимости между признаками и target (построили некоторые гипотезы)

# **Часть 2 | Модель только на вещественных признаках**

Сделали линейную регрессию в пункте 9:
Линейная регрессия показала среднее качество по R2 и схожие значения метрик на train и test, что говорит об отсутствии сильного переобучения. MSE был выше на тестовых данных, что ожидаемо, тк модель обучалась на train.

Добавили стандартизацию к линейной регрессии в пункте 10: она не привела к улучшению качества по метрикам (но возможно полезна с точки зрения интерпретации коэффициентов)

В задании 12 сделали регуляризацию с применением модели Lasso:
Метрики качества оказались практически равными значениям в предыдущей итерации модели. Качество такое же - среднее (R2 около 0.59). L1-регуляризация не занулила веса. Видимо это потому что параметры по умолчанию слишком малы и регуляризованная модель почти не отличается от изначальной. Для усиления регуляризации можно подбирать параметры, но этого не требовалось в задаче (можно сделать в будущем)

В задании 13 построили оптимальные модели Lasso, ElasticNet c CV. Но r2 немного упал Эта итерация не особо помогла улучшить модель судя по R2.

# **Часть 3| Добавляем категориальные фичи**
Работа с категориальными фичами как ни странно тоже не особо помогла улучшить качество модели судя по метрике R2 (строили модификацию Ridge)

2) Оценка разработанного сервиса:

В принципе нормально получилось - модель предсказывает цены автомобилей по заданным данным. На мой взгляд, EDA по каким-то внешним данным делать в сервисе не очень рационально, поэтому я сделала только начало EDA. 
